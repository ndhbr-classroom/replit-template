import subprocess

# Sets git config by name and email
def set_git_config(name, mail):
    f = open("/home/runner/.gitconfig", "w")
    f.writelines([
        "[user]",
        f"\n\tname = {name}",
        f"\n\temail = {mail}",
    ])
    f.close()